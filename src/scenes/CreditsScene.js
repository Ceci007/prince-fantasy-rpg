/* eslint-disable no-undef */
import 'phaser';

export default class CreditsScene extends Phaser.Scene { // eslint-disable-line
  constructor() {
    super('Score');
  }

  // preload() {}

  // create() {}
}